version: '3'

services:
    db:
        image: mariadb
        ports:
            - '3306:3306'
        environment:
            MYSQL_ROOT_PASSWORD: Welkom01!
    web:
        image: nginx
        volumes:
            - '.:/usr/share/nginx/html'
        ports:
            - '80:80'
